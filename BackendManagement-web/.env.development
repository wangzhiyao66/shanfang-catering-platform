# 开发环境：Vite dev server 通过 proxy 把 /api 转发到后端 :3000
VITE_API=/api
VITE_APP_TITLE=膳房·商家后台
# 按路径兜底 Mock：true 时，后端已实现的接口（dishes/orders/tables/members 等）走真实后端，
# 后端尚未实现的模块（dashboard/report/employee/marketing/setting/shop/订单详情）由 mock 兜底。
# 全部接口就绪后可改为 false 完全走真实后端。
VITE_USE_MOCK=true
